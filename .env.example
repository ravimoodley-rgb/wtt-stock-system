# Copy to .env and adjust for your deployment
PORT=3001
# Force redirect http -> https (set "true" when behind a TLS-terminating proxy/CDN)
FORCE_HTTPS=false
# Trust the proxy's X-Forwarded-* headers (set "true" behind a proxy/CDN)
TRUST_PROXY=false