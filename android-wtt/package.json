{
  "name": "wtt-android",
  "version": "1.0.0",
  "private": true,
  "description": "Android wrapper for WTT Mobile stock capture",
  "scripts": {
    "android:sync": "cap sync android",
    "android:open": "cap open android",
    "android:build": "cd android && gradlew assembleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0",
    "typescript": "^7.0.2"
  }
}
